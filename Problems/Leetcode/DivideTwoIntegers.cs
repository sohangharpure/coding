using System;
using System.Collections.Generic;
using System.Text;

namespace Problems.Leetcode
{
    //https://leetcode.com/problems/divide-two-integers/
    //Given two integers dividend and divisor, divide two integers without using multiplication, division and mod operator.
    //Return the quotient after dividing dividend by divisor.
    //The integer division should truncate toward zero.
    class DivideTwoIntegersSolution
    {
        /*public int Divide(int dividend, int divisor)
        {
            if (dividend < Int32.MinValue || dividend > Int32.MaxValue)
                return 0;

            if (divisor < Int32.MinValue || divisor > Int32.MaxValue)
                return 0;

            if (divisor == 0)
                return 0;

            int result = 0;
            int sign_of_dividend = dividend / Math.Abs(dividend);
            int sign_of_divisor = divisor / Math.Abs(divisor);
            int temp_dividend = Math.Abs(dividend);
            int temp_divisor = Math.Abs(divisor);
            

        }*/
    }
}
